[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/config"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[[services]]
name = "web"

[services.web]
source = "."